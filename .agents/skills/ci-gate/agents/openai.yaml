interface:
  display_name: "CI Gate"
  short_description: "Run local CI parity checks"
  default_prompt: "Use $ci-gate to run local CI checks and summarize any failures with fixes."

policy:
  allow_implicit_invocation: true
