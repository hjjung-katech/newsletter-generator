interface:
  display_name: "Scheduler Debug"
  short_description: "Debug scheduler idempotency"
  default_prompt: "Use $scheduler-debug to analyze schedule windows, deterministic idempotency keys, duplicate prevention, and fallback behavior."

policy:
  allow_implicit_invocation: false
