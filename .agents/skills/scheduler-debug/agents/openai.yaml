interface:
  display_name: "Scheduler Debug"
  short_description: "Debug schedule execution logic"
  default_prompt: "Use $scheduler-debug to analyze schedule windows, duplicate prevention, and fallback execution behavior."

policy:
  allow_implicit_invocation: false
