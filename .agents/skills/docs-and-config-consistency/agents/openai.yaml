interface:
  display_name: "Docs Config Sync"
  short_description: "Lock config safety contracts"
  default_prompt: "Use $docs-and-config-consistency to enforce canonical env names, remove import side effects, and align docs/runtime config."

policy:
  allow_implicit_invocation: true
