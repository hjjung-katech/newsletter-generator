# ========================================
# Newsletter Generator Environment Variables
# F-14 Centralized Settings Layer
# ========================================

# ==========================================
# [Required] 필수 환경변수 - 앱 시작에 필요
# ==========================================

# News Search API (필수)
SERPER_API_KEY=sk-your-serper-api-key-here-32chars-minimum

# Email Service (필수)
POSTMARK_SERVER_TOKEN=your-postmark-server-token-here-32chars-minimum
EMAIL_SENDER=noreply@yourdomain.com

# LLM API Keys (하나 이상 필요)
OPENAI_API_KEY=sk-your-openai-api-key-here-32chars-minimum
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here-32chars-minimum
GEMINI_API_KEY=your-gemini-api-key-here-32chars-minimum

# ==========================================
# [Optional] 선택적 환경변수 - 기본값 있음
# ==========================================

# Application Environment
# Values: development, testing, production
APP_ENV=development

# Web Server Configuration
PORT=8000
HOST=0.0.0.0

# Security
SECRET_KEY=dev-secret-key-change-in-production-min-16-chars

# Logging Configuration
# Values: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# Values: json, text
LOG_FORMAT=json

# Application Modes
MOCK_MODE=false
DEBUG=false

# Monitoring & Error Tracking (Optional)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
SENTRY_TRACES_SAMPLE_RATE=0.1
SENTRY_PROFILES_SAMPLE_RATE=0.1

# Infrastructure (Optional)
REDIS_URL=redis://localhost:6379/0
RQ_QUEUE=default
DATABASE_URL=sqlite:///storage.db

# Google Services (Optional)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Naver API (Optional)
NAVER_CLIENT_ID=your-naver-client-id
NAVER_CLIENT_SECRET=your-naver-client-secret

# Newsletter Configuration
DEFAULT_PERIOD=14
DEFAULT_TEMPLATE_STYLE=compact
ADDITIONAL_RSS_FEEDS=https://example.com/feed1.xml,https://example.com/feed2.xml

# Testing & Deployment
TEST_BASE_URL=http://localhost:5000
TEST_EMAIL_RECIPIENT=test@example.com
RAILWAY_PRODUCTION_URL=https://your-app.railway.app
PRODUCTION_URL=https://your-app.example.com
APP_VERSION=1.0.0
ENVIRONMENT=production

# ==========================================
# 환경별 설정 예시
# ==========================================

# Development (.env)
# - APP_ENV=development
# - DEBUG=true
# - LOG_LEVEL=DEBUG
# - MOCK_MODE=true

# Testing (GitHub Actions)
# - APP_ENV=testing
# - MOCK_MODE=true
# - 시크릿은 GitHub Secrets에서 주입

# Production (Docker/Railway)
# - APP_ENV=production
# - DEBUG=false
# - LOG_LEVEL=INFO
# - 시크릿은 환경변수로 주입, .env 파일 불필요

# ==========================================
# 보안 주의사항
# ==========================================
# 1. 이 파일은 예시용입니다 - 실제 키는 입력하지 마세요!
# 2. .env 파일은 절대 git에 커밋하지 마세요
# 3. 프로덕션에서는 OS 환경변수 사용
# 4. API 키는 최소 16자 이상 (권장: 32자+)
# 5. 시크릿 순환(rotation) 정기적으로 수행 