{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Flask Server",
            "type": "shell",
            "command": "python app.py",
            "group": "build",
            "isBackground": true,
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/web"
            }
        }
    ]
}